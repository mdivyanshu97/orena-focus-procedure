# Runtime resources

The Python files and prompt resources in this directory were recovered from the
selected submission image.

The two merged models are intentionally excluded from Git. Run:

```bash
python scripts/download_weights.py
```

from the repository root to populate `resources/w64/` and `resources/nw2/`
from the public Hugging Face release.
